import { requiredAdmin } from "@/lib/auth"

    export default async function Dashboard() {
        
        return (
            <div>
                <h1>Deshboard</h1>
            </div>
        )
    }